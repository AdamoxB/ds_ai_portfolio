/* 1️⃣  Kolory pochodzą z motywu (zmieniane przy przełączniku) */
body {
    background: radial-gradient(circle at center,
        var(--md-primary-fg), var(--md-accent-fg));
    color: var(--md-text-color);
}

/* 2️⃣  Nagłówki – korzystają ze zmiennych motywu */
h1, h2, h3, .md-header__title {
    font-family: 'Press Start 2P', cursive;
    color: var(--md-primary-fg);   /* zmienia się w trybie ciemnym */
}

/* 3️⃣  Animacja linków – nie zależy od koloru */
a { position: relative; display:inline-block; transition:.3s ease;}
a::after {
    content:""; position:absolute; width:100%; transform:scaleX(0);
    height:2px; bottom:-4px; left:0;
    background-color:var(--md-primary-fg); /* używamy zmiennej */
    transform-origin:bottom right; transition:transform .25s ease-out;
}
a:hover::after { transform:scaleX(1); transform-origin:bottom left;}

