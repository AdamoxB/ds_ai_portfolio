/* Pozycjonowanie przycisku w prawym górnym rogu */
.md-header__inner .md-toggle.secondary {
  margin-left: auto;
  display: flex;
}

/* Przykład, co się zmienia po kliknięciu */
body.custom-theme .md-content {
  background-color: #f0f8ff;   /* lekko niebieskie tło */
}
